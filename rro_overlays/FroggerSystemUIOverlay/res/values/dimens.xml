<?xml version="1.0" encoding="utf-8"?>
<resources>
    <dimen name="keyguard_indication_margin_bottom">350px</dimen>

    <!-- Location on the screen of the center of the physical power button. This is a reasonable
    default that should be overridden by device-specific overlays. -->
    <dimen name="physical_power_button_center_screen_location_y">1030px</dimen>

    <!-- The padding on the top of the statusbar (usually 0).

         0 rather than a nudge: with status_bar_height telling the truth about the
         162px the punch hole imposes, centring puts the content at y=81, which is
         the hole's own centre (its path spans 47.6..115.5). Any padding here pushes
         the real status bar down without moving the shade header, which is what
         made the two disagree. -->
    <dimen name="status_bar_padding_top">0px</dimen>

    <!-- The padding on the start of the status bar. -->
    <dimen name="status_bar_padding_start">48px</dimen>

    <!-- Microns/ums (1000 um = 1mm) per pixel for the given device. If unspecified, UI that
         relies on this value will not be sized correctly. -->
    <item name="pixel_pitch" format="float" type="dimen">50.0</item>

    <!-- Udfps animation vertical offset -->
    <dimen name="udfps_animation_offset">-20px</dimen>
</resources>
